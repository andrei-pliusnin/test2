import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("QR Scanner App")
            .font(.title)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
